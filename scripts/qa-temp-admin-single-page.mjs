import { spawn } from "node:child_process";
import { createServer } from "node:http";
import { existsSync } from "node:fs";
import { mkdir, readFile, rm, stat, writeFile } from "node:fs/promises";
import path from "node:path";
import { fileURLToPath } from "node:url";

const rootDir = path.resolve(path.dirname(fileURLToPath(import.meta.url)), "..");
const profileDir = "/tmp/irum-temp-admin-chromium";
const outputDir = "/tmp/irum-temp-admin-qa";
const chromiumPath = process.env.CHROMIUM_PATH || ["/usr/bin/chromium", "/usr/bin/chromium-browser", "/usr/bin/google-chrome"].find(existsSync);
if (!chromiumPath) throw new Error("Chromium executable not found");
const port = 4176;
const debugPort = 9334;
const mime = new Map([[".html","text/html; charset=utf-8"],[".js","text/javascript; charset=utf-8"],[".css","text/css; charset=utf-8"],[".png","image/png"],[".jpg","image/jpeg"],[".jpeg","image/jpeg"],[".webp","image/webp"],[".svg","image/svg+xml"]]);

await rm(profileDir,{recursive:true,force:true});
await rm(outputDir,{recursive:true,force:true});
await mkdir(outputDir,{recursive:true});

const server=createServer(async(request,response)=>{try{const pathname=decodeURIComponent(new URL(request.url||"/",`http://127.0.0.1:${port}`).pathname),candidate=pathname.endsWith("/")?`${pathname}index.html`:pathname,filePath=path.resolve(rootDir,`.${candidate}`);if(!filePath.startsWith(`${rootDir}${path.sep}`))throw new Error("PATH_OUTSIDE_ROOT");if(!(await stat(filePath)).isFile())throw new Error("NOT_A_FILE");response.writeHead(200,{"Content-Type":mime.get(path.extname(filePath).toLowerCase())||"application/octet-stream"});response.end(await readFile(filePath));}catch{response.writeHead(404,{"Content-Type":"text/plain; charset=utf-8"});response.end("Not found")}});
await new Promise(resolve=>server.listen(port,"127.0.0.1",resolve));

const chromium=spawn(chromiumPath,["--headless=new","--no-sandbox","--disable-gpu","--disable-dev-shm-usage",`--remote-debugging-port=${debugPort}`,`--user-data-dir=${profileDir}`,"about:blank"],{stdio:["ignore","ignore","ignore"]});
const sleep=ms=>new Promise(resolve=>setTimeout(resolve,ms));
async function waitDebug(){for(let i=0;i<100;i+=1){try{if((await fetch(`http://127.0.0.1:${debugPort}/json/version`)).ok)return}catch{}await sleep(100)}throw new Error("Chromium debugging port did not open")}
await waitDebug();
const target=await fetch(`http://127.0.0.1:${debugPort}/json/new?about:blank`,{method:"PUT"}).then(r=>r.json());
const socket=new WebSocket(target.webSocketDebuggerUrl);
await new Promise((resolve,reject)=>{socket.addEventListener("open",resolve,{once:true});socket.addEventListener("error",reject,{once:true})});
let commandId=0;const pending=new Map();
socket.addEventListener("message",event=>{const message=JSON.parse(String(event.data));if(!message.id||!pending.has(message.id))return;const job=pending.get(message.id);pending.delete(message.id);message.error?job.reject(new Error(message.error.message)):job.resolve(message.result)});
function send(method,params={}){const id=++commandId;socket.send(JSON.stringify({id,method,params}));return new Promise((resolve,reject)=>pending.set(id,{resolve,reject}))}
async function evaluate(expression){const result=await send("Runtime.evaluate",{expression,returnByValue:true,awaitPromise:true});if(result.exceptionDetails)throw new Error(result.exceptionDetails.exception?.description||result.exceptionDetails.text||"Browser evaluation failed");return result.result.value}
async function waitFor(expression,label,timeout=20000){const start=Date.now();while(Date.now()-start<timeout){if(await evaluate(`Boolean(${expression})`))return;await sleep(120)}throw new Error(`Timed out waiting for ${label}`)}
async function screenshot(file){const metrics=await send("Page.getLayoutMetrics"),width=Math.ceil(metrics.cssContentSize?.width||metrics.contentSize.width),height=Math.min(12000,Math.ceil(metrics.cssContentSize?.height||metrics.contentSize.height)),result=await send("Page.captureScreenshot",{format:"png",captureBeyondViewport:true,fromSurface:true,clip:{x:0,y:0,width,height,scale:1}});await writeFile(file,Buffer.from(result.data,"base64"))}

try{
  await send("Page.enable");await send("Runtime.enable");await send("Network.enable");
  await send("Emulation.setDeviceMetricsOverride",{width:1440,height:900,deviceScaleFactor:1,mobile:false});
  await send("Page.navigate",{url:`http://127.0.0.1:${port}/admin/`});
  await waitFor("document.readyState === 'complete'","admin document");
  await waitFor("document.querySelector('#login-button')","unauthenticated login gate");
  const unauthenticated=await evaluate(`({login:!!document.querySelector('#login-button'),layout:!!document.querySelector('.layout')})`);

  await evaluate(`(async()=>{
    sessionStorage.setItem(sessionKey,JSON.stringify({access_token:'qa-token',expires_at:Math.floor(Date.now()/1000)+3600}));
    sessionStorage.setItem(issuedLinksKey,JSON.stringify({'qa-form':'https://irum.click/profile/#qa-token'}));
    window.__qaCalls=[];window.confirm=()=>true;window.open=()=>null;
    photoUrl=async()=> 'data:image/svg+xml;charset=utf-8,'+encodeURIComponent('<svg xmlns="http://www.w3.org/2000/svg" width="320" height="420"><rect width="100%" height="100%" fill="#d8d0c5"/><text x="50%" y="50%" text-anchor="middle" fill="#28231c">PRIVATE QA</text></svg>');
    loadAll=async()=>{};
    state.records=[
      {id:2,created_at:'2026-08-27T01:00:00.000Z',payload:{submission_type:'matching',profile:{name:'QA 남성',phone:'01011112222',birthYear:'1992',gender:'male',job:'대표',education:'대졸',region:'서울',height:'180',mbti:'ENTJ',appealPoints:['리더십']},photo_refs:[{path:'submissions/2/qa.jpg'}]}},
      {id:3,created_at:'2026-08-27T02:00:00.000Z',payload:{submission_type:'social',profile:{name:'QA 복수',phone:'01033334444',birthYear:'1995',gender:'female',job:'마케터',region:'서울',socialAttendanceIntent:'specific_event',socialEventId:'event-1'},photo_refs:[]}},
      {id:4,created_at:'2026-08-27T03:00:00.000Z',payload:{submission_type:'matching',profile:{name:'QA 복수',phone:'01033334444',birthYear:'1995',gender:'female',job:'마케터',region:'서울'},photo_refs:[]}}
    ];
    state.legacy=[{source_application_id:1,source_updated_at:'2026-08-26T00:00:00.000Z',exported_at:'2026-08-26T00:00:00.000Z',photo_refs:[],snapshot:{profile:{name:'QA 여성',phone:'01000000000',birthYear:'1994',gender:'female',job:'전문직',education:'대졸',region:'서울',height:'165',mbti:'INFJ',entryPath:'/matching'},consultation:{consultationStatus:'before'},notes:[{content:'기존 상담 메모',source:'원본 CRM',createdAt:'2026-08-25T00:00:00.000Z'}]}}];
    state.operational=[{source_type:'social_event',source_id:'event-1',payload:{title:'QA 프라이빗 소셜',startsAt:'2026-10-17T10:00:00.000Z',location:'서울 강남',participationFee:50000,status:'OPEN'}},{source_type:'member',source_id:'legacy-member-1',payload:{applicant_name:'기존 회원',applicant_gender:'male',memberStatus:'active',applicant_job:'전문직',applicant_region:'서울'}},{source_type:'task',source_id:'task-1',payload:{title:'기존 할 일'}},{source_type:'schedule',source_id:'schedule-1',payload:{title:'기존 일정'}},{source_type:'audit',source_id:'audit-1',payload:{action:'legacy'}}];
    state.consultations=[];
    state.forms=[
      {id:'qa-form',subject_type:'temporary_submission',subject_id:'2',form_type:'profile_male',status:'submitted',expires_at:'2026-09-10T00:00:00.000Z',created_at:'2026-08-27T00:00:00.000Z',submitted_at:'2026-08-27T00:30:00.000Z',sent_at:null,draft_revision:3,submitted_payload:{birthDate:'1992-01-01',incomeMale:'8천만원',asset:'3~5억원',purpose:'marriage',documentDeferred:false,privacyConsent:true}},
      {id:'qa-female-form',subject_type:'legacy_snapshot',subject_id:'1',form_type:'profile_female',status:'submitted',expires_at:'2026-09-10T00:00:00.000Z',created_at:'2026-08-26T00:00:00.000Z',submitted_at:'2026-08-26T01:00:00.000Z',sent_at:'2026-08-26T00:30:00.000Z',submitted_payload:{birthDate:'1994-01-01',realCheckMethod:'video',realCheckDate:'2026-09-15',serviceSelection:'matching',privacyConsent:true}},
      {id:'qa-social-form',subject_type:'temporary_submission',subject_id:'3',form_type:'social_event',status:'submitted',expires_at:'2026-09-10T00:00:00.000Z',created_at:'2026-08-27T02:30:00.000Z',submitted_at:'2026-08-27T02:45:00.000Z',sent_at:'2026-08-27T02:35:00.000Z',submitted_payload:{job:'마케터',workplace:'QA 회사',privacyConsent:true}}
    ];
    state.documents=[{id:'qa-document',form_id:'qa-form',document_type:'job',status:'verified'}];
    state.reviews=[{id:'review-social',form_id:'qa-social-form',subject_type:'temporary_submission',subject_id:'3',result:'approved',reason:null,reviewed_by_email:'qa-admin@example.invalid',reviewed_at:'2026-08-27T03:30:00.000Z'},{id:'review-male',form_id:'qa-form',subject_type:'temporary_submission',subject_id:'2',result:'approved',reason:null,reviewed_by_email:'qa-admin@example.invalid',reviewed_at:'2026-08-27T02:00:00.000Z'},{id:'review-female',form_id:'qa-female-form',subject_type:'legacy_snapshot',subject_id:'1',result:'approved',reason:null,reviewed_by_email:'qa-admin@example.invalid',reviewed_at:'2026-08-26T02:00:00.000Z'}];
    state.secondaryEvents=[];state.workflows=[];state.workflowEvents=[];state.schedules=[{id:'schedule-old',subject_type:'temporary_submission',subject_id:'2',schedule_type:'consultation',event_action:'created',scheduled_at:'2026-10-10T10:00:00.000Z',actor_email:'qa-admin@example.invalid',created_at:'2026-08-26T01:00:00.000Z'},{id:'schedule-new',subject_type:'temporary_submission',subject_id:'2',schedule_type:'consultation',event_action:'updated',scheduled_at:'2026-10-20T10:00:00.000Z',actor_email:'qa-admin@example.invalid',created_at:'2026-08-27T01:00:00.000Z'},{id:'schedule-cancel',subject_type:'legacy_snapshot',subject_id:'1',schedule_type:'next_contact',event_action:'cancelled',scheduled_at:null,actor_email:'qa-admin@example.invalid',created_at:'2026-08-27T02:00:00.000Z'}];state.members=[{id:'member-male',subject_type:'temporary_submission',subject_id:'2',review_id:'review-male',member_status:'converted',actor_email:'qa-admin@example.invalid',created_at:'2026-08-27T03:00:00.000Z'},{id:'member-female',subject_type:'legacy_snapshot',subject_id:'1',review_id:'review-female',member_status:'matchable',actor_email:'qa-admin@example.invalid',created_at:'2026-08-27T03:00:00.000Z'}];state.matchingCases=[{id:'meeting-completed-1',male_subject_type:'temporary_submission',male_subject_id:'2',female_subject_type:'legacy_snapshot',female_subject_id:'1',status:'meeting_completed',updated_at:'2026-08-27T03:00:00.000Z',created_at:'2026-08-27T03:00:00.000Z'}];state.matchingEvents=[];state.socialEvents=[{id:'social-selected',social_event_id:'event-1',subject_type:'temporary_submission',subject_id:'3',previous_status:'reviewing',status:'selected',actor_email:'qa-admin@example.invalid',created_at:'2026-08-27T03:00:00.000Z'}];state.legacySocialEvents=[];state.audit=[];state.fieldCorrections=[];state.phoneConsultations=[];state.internalEvaluations=[];state.matchingFeedback=[];state.loadedAt=Date.now();
    invokeAdmin=async(action,body={})=>{window.__qaCalls.push({endpoint:'intake',action,body});const now=new Date().toISOString();if(action==='admin-workflow-set'){state.workflows.unshift({id:'wf',...body,updated_at:now,assigned_to:body.assigned_to||null})}else if(action==='admin-schedule-add'){const previous=state.schedules.find(e=>e.subject_type===body.subject_type&&String(e.subject_id)===String(body.subject_id)&&e.schedule_type===body.schedule_type);state.schedules.unshift({id:'schedule',...body,event_action:previous?'updated':'created',scheduled_at:body.scheduled_at,actor_email:'qa-admin@example.invalid',created_at:now})}else if(action==='admin-schedule-cancel'){state.schedules.unshift({id:'schedule-cancel',...body,event_action:'cancelled',scheduled_at:null,actor_email:'qa-admin@example.invalid',created_at:now})}else if(action==='consultation-add'){state.consultations.unshift({id:'memo',...body,created_by_email:'qa-admin@example.invalid',created_at:now})}else if(action==='admin-member-set'){const previous=state.members.find(e=>e.subject_type===body.subject_type&&String(e.subject_id)===String(body.subject_id));state.members.unshift({id:'member',...body,previous_status:previous?.member_status||'approval_pending',actor_email:'qa-admin@example.invalid',created_at:now})}else if(action==='admin-match-create'){state.matchingCases.unshift({id:'match-1',male_subject_type:body.male_subject_type,male_subject_id:body.male_subject_id,female_subject_type:body.female_subject_type,female_subject_id:body.female_subject_id,status:'candidate_selected',updated_at:now,created_at:now});state.members.unshift({id:'matching-male',subject_type:body.male_subject_type,subject_id:body.male_subject_id,member_status:'matching',created_at:now},{id:'matching-female',subject_type:body.female_subject_type,subject_id:body.female_subject_id,member_status:'matching',created_at:now})}else if(action==='admin-match-transition'){const c=state.matchingCases.find(x=>x.id===body.matching_case_id);if(c){c.status=body.status;c.updated_at=now}state.matchingEvents.unshift({id:'match-event',matching_case_id:body.matching_case_id,status:body.status,reason:body.reason,scheduled_at:body.scheduled_at,actor_email:'qa-admin@example.invalid',created_at:now})}else if(action==='admin-social-status-set'){const previous=state.socialEvents.find(e=>e.social_event_id===body.social_event_id&&e.subject_type===body.subject_type&&String(e.subject_id)===String(body.subject_id));state.socialEvents.unshift({id:'social-'+body.status,...body,previous_status:previous?.status||'applied',actor_email:'qa-admin@example.invalid',created_at:now})}else if(action==='admin-field-correction-add'){state.fieldCorrections.unshift({id:'correction-1',...body,actor_email:'qa-admin@example.invalid',created_at:now})}else if(action==='admin-phone-consultation-save'){state.phoneConsultations.unshift({id:'phone-1',...body,actor_email:'qa-admin@example.invalid',created_at:now})}else if(action==='admin-internal-evaluation-save'){state.internalEvaluations.unshift({id:'internal-1',...body,actor_email:'qa-admin@example.invalid',created_at:now})}else if(action==='admin-matching-feedback-add'){state.matchingFeedback.unshift({id:'feedback-1',...body,actor_email:'qa-admin@example.invalid',created_at:now})}return {ok:true};};
    api=async(endpoint,action,body={})=>{window.__qaCalls.push({endpoint:'secondary',action,body});const now=new Date().toISOString();if(action==='secondary-admin-review'){state.reviews.unshift({id:'review-male',form_id:body.form_id,subject_type:'temporary_submission',subject_id:'2',result:body.result,reason:body.reason,reviewed_by_email:'qa-admin@example.invalid',reviewed_at:now});return {review:state.reviews[0]}}if(action==='secondary-admin-mark-sent'){const f=state.forms.find(x=>x.id===body.form_id);f.sent_at=now;f.sent_by_email='qa-admin@example.invalid';return {form:f}}if(action==='secondary-admin-clear-sent'){const f=state.forms.find(x=>x.id===body.form_id);f.sent_at=null;f.sent_by_email=null;return {form:f}}if(action==='secondary-admin-document-url')return {signed_url:'https://example.invalid/private'};if(action==='secondary-admin-reissue'){const f=state.forms.find(x=>x.id===body.form_id);f.sent_at=null;return {form:f,raw_url:'https://irum.click/profile/#qa-new-token'}}throw Object.assign(new Error('UNEXPECTED_QA_ACTION'),{code:'UNEXPECTED_QA_ACTION'});};
    state.page='applicants';renderApplicants();return true;
  })()`);
  await waitFor("document.querySelectorAll('[data-subject]').length === 3","fixture applicant list");
  const list=await evaluate(`({cards:document.querySelectorAll('[data-subject]').length,filters:document.querySelectorAll('.filters input,.filters select').length,duplicateCards:Array.from(document.querySelectorAll('.app-card')).filter(n=>n.innerText.includes('동일인 그룹')).length,oldMenu:Array.from(document.querySelectorAll('[data-nav]')).some(n=>/티어 확인|Host 유입|통계|관리자 · 권한/.test(n.textContent)),labels:Array.from(document.querySelectorAll('[data-nav]')).map(n=>n.textContent.trim()),overflow:document.documentElement.scrollWidth>document.documentElement.clientWidth+1})`);
  const filterQuery=await evaluate(`(()=>{state.filters={q:'QA',stage:'member_converted',service:'matching',sent:'all',submitted:'submitted',gender:'male',owner:'all',source:'temporary',duplicate:'no'};writeFilterQuery();renderApplicants();const result={search:location.search,count:filteredItems().length,display:document.querySelector('[data-filter-result-count]').textContent,cards:document.querySelectorAll('[data-subject]').length};state.filters={q:'',stage:'all',service:'all',sent:'all',submitted:'all',gender:'all',owner:'all',source:'all',duplicate:'all'};writeFilterQuery();renderApplicants();return result})()`);
  await screenshot(`${outputDir}/desktop-list.png`);

  await evaluate(`document.querySelector('[data-open="phone:01011112222"]').click()`);
  await waitFor("document.querySelectorAll('[data-temp-applicant-section]').length === 5","five applicant sections");
  await waitFor("document.querySelector('.profile-photo img')","private photo thumbnail");
  const detail=await evaluate(`(()=>{const answers=document.querySelector('[data-temp-applicant-section="secondary-responses"]').innerText;return{sections:Array.from(document.querySelectorAll('[data-temp-applicant-section]')).map(n=>[n.dataset.tempApplicantSection,Number(n.dataset.tempSectionOrder)]),review:!!document.querySelector('[data-review-save]'),document:!!document.querySelector('[data-document]'),markSent:!!document.querySelector('[data-mark-sent]'),copy:!!document.querySelector('[data-copy-link]'),privatePhoto:!!document.querySelector('.profile-photo img'),currentWork:document.body.innerText.includes('CURRENT WORK'),koreanAnswers:answers.includes('생년월일')&&answers.includes('연소득')&&answers.includes('개인 순자산')&&answers.includes('결혼')&&answers.includes('개인정보 수집·2차 프로필 활용 동의')&&answers.includes('직업 인증서류 · 확인 완료'),rawDuplicate:/\b(name|phone|gender)\b/.test(answers),overflow:document.documentElement.scrollWidth>document.documentElement.clientWidth+1}})()`);

  await evaluate(`(()=>{const box=document.querySelector('[data-review-box]');box.querySelector('[data-review-result]').value='hold';box.querySelector('[data-review-save]').click()})()`);
  await waitFor("document.querySelector('.toast')","review reason validation");
  const callsBeforeReason=await evaluate(`window.__qaCalls.filter(c=>c.action==='secondary-admin-review').length`);
  await evaluate(`(()=>{const box=document.querySelector('[data-review-box]');box.querySelector('[data-review-reason]').value='추가 확인 필요';box.querySelector('[data-review-save]').click()})()`);
  await waitFor("document.body.innerText.includes('추가 확인 필요')","append-only review history");
  await evaluate(`(()=>{const box=document.querySelector('[data-review-box]');box.querySelector('[data-review-result]').value='approved';box.querySelector('[data-review-reason]').value='';box.querySelector('[data-review-save]').click()})()`);
  await waitFor("document.body.innerText.includes('승인') && document.querySelector('[data-review-box] .chip.green')","approved review transition");
  const reviewCalls=await evaluate(`window.__qaCalls.filter(c=>c.action==='secondary-admin-review').length`);

  await evaluate(`document.querySelector('[data-mark-sent]').click()`);
  await waitFor("document.querySelector('[data-clear-sent]')","manual sent mark");
  await evaluate(`document.querySelector('[data-clear-sent]').click()`);
  await waitFor("document.querySelector('[data-mark-sent]')","manual sent clear");
  const sentCalls=await evaluate(`(()=>{const mark=window.__qaCalls.filter(c=>c.action==='secondary-admin-mark-sent'),clear=window.__qaCalls.filter(c=>c.action==='secondary-admin-clear-sent');return{mark:mark.length,clear:clear.length,markBody:mark[0]?.body,clearBody:clear[0]?.body}})()`);

  await evaluate(`(()=>{const input=document.querySelector('#consultation-at');input.value='2026-10-20T15:00';document.querySelector('[data-save-schedule="consultation"]').click()})()`);
  await waitFor("document.body.innerText.includes('2026. 10. 20')","consultation schedule saved");
  await evaluate(`(()=>{const input=document.querySelector('#next-contact-at');input.value='2026-10-18T11:00';document.querySelector('[data-save-schedule="next_contact"]').click()})()`);
  await waitFor("document.body.innerText.includes('2026. 10. 18')","next-contact schedule saved");
  const scheduleTypes=await evaluate(`window.__qaCalls.filter(c=>c.action==='admin-schedule-add').map(c=>c.body.schedule_type)`);
  await screenshot(`${outputDir}/desktop-detail.png`);

  await evaluate(`(()=>{const field=document.querySelector('#correction-field');field.value='job';field.dispatchEvent(new Event('change'));document.querySelector('#correction-value').value='QA 정정 직업';document.querySelector('#correction-save').click()})()`);await waitFor("state.fieldCorrections.length===1","field correction revision");
  await evaluate(`(()=>{document.querySelector('[data-phone-field="marriageView"]').value='QA 결혼관 확인';document.querySelector('#phone-consultation-form button').click()})()`);await waitFor("state.phoneConsultations.length===1","phone consultation revision");
  await evaluate(`(()=>{document.querySelector('[data-crm-tab="internal"]').click();document.querySelector('[data-internal-field]').value='QA 내부 평가';document.querySelector('#internal-evaluation-form button').click()})()`);await waitFor("state.internalEvaluations.length===1","internal evaluation revision");
  await evaluate(`(()=>{document.querySelector('[data-crm-tab="feedback"]').click();document.querySelector('#feedback-meeting-at').value='2026-10-22T19:30';document.querySelector('#feedback-positive').value='대화,매너';document.querySelector('#feedback-negative').value='없음';document.querySelector('#feedback-adjustment').value='다음 매칭 참고';document.querySelector('#matching-feedback-form button').click()})()`);await waitFor("state.matchingFeedback.length===1","matching feedback revision");
  const crmDetail=await evaluate(`(()=>{const calls=window.__qaCalls;const feedback=calls.find(c=>c.action==='admin-matching-feedback-add');return{rail:!!document.querySelector('.change-rail'),railText:document.querySelector('.change-rail')?.innerText||'',corrections:state.fieldCorrections.length,phone:state.phoneConsultations.length,internal:state.internalEvaluations.length,feedback:state.matchingFeedback.length,primaryJob:state.records.find(r=>r.id===2)?.payload?.profile?.job,feedbackSubject:feedback?.body?.feedback_subject_type+':'+feedback?.body?.feedback_subject_id,providerSubject:feedback?.body?.provider_subject_type+':'+feedback?.body?.provider_subject_id,feedbackCase:feedback?.body?.matching_case_id,correctionCalls:calls.filter(c=>c.action==='admin-field-correction-add').length,phoneCalls:calls.filter(c=>c.action==='admin-phone-consultation-save').length,internalCalls:calls.filter(c=>c.action==='admin-internal-evaluation-save').length}})()`);
  await evaluate(`navigate('members')`);await waitFor("document.querySelector('[data-member-save]')","approved members page");
  const membersBefore=await evaluate(`({cards:document.querySelectorAll('[data-member-save]').length,hasPending:document.body.innerText.includes('승인 대기'),oldSnapshot:document.body.innerText.includes('회원 Snapshot'),pendingNext:Array.from(document.querySelectorAll('[data-member-item]')).find(n=>n.innerText.includes('QA 복수'))?.querySelector('[data-member-status]')?.value})`);
  await evaluate(`document.querySelector('[data-member-item="phone:01033334444"] [data-member-save]').click()`);await waitFor("state.members.some(e=>e.subject_id==='3'&&e.member_status==='converted')","member converted transition");
  await evaluate(`document.querySelector('[data-member-item="phone:01033334444"] [data-member-save]').click()`);await waitFor("state.members.some(e=>e.subject_id==='3'&&e.member_status==='matchable')","member matchable transition");
  await evaluate(`document.querySelector('[data-member-item="phone:01011112222"] [data-member-save]').click()`);await waitFor("state.members.some(e=>e.subject_id==='2'&&e.member_status==='matchable')","male matchable transition");
  const members=await evaluate(`({before:state.members.find(e=>e.subject_id==='3'&&e.member_status==='converted')?.previous_status,latestMale:latest(state.members.filter(e=>e.subject_id==='2'))?.member_status,latestSocial:latest(state.members.filter(e=>e.subject_id==='3'))?.member_status})`);

  await evaluate(`navigate('matching')`);await waitFor("document.querySelector('#match-create')","matching page");
  const candidateOptions=await evaluate(`({men:Array.from(document.querySelectorAll('#match-male option')).map(o=>o.value).filter(Boolean),women:Array.from(document.querySelectorAll('#match-female option')).map(o=>o.value).filter(Boolean)})`);
  await evaluate(`(()=>{const m=document.querySelector('#match-male'),f=document.querySelector('#match-female');m.value='temporary_submission:2';f.value='legacy_snapshot:1';document.querySelector('#repeat-confirm').checked=true;document.querySelector('#match-create').click()})()`);
  await waitFor('document.querySelector(\'[data-match="match-1"]\')',"male-choice match case");
  const matching=await evaluate(`({cases:document.querySelectorAll('[data-match]').length,hasFemaleDecision:/여성\s*(수락|거절)/.test(Array.from(document.querySelectorAll('option')).map(o=>o.textContent).join(' ')),statuses:Array.from(document.querySelectorAll('[data-match-status] option')).map(o=>o.textContent),maleLatest:latest(state.members.filter(e=>e.subject_id==='2'))?.member_status,femaleLatest:latest(state.members.filter(e=>e.subject_type==='legacy_snapshot'&&e.subject_id==='1'))?.member_status})`);

  await evaluate(`navigate('operations')`);await waitFor("document.body.innerText.includes('운영 일정')","latest schedule operations");
  const schedules=await evaluate(`({newDate:document.body.innerText.includes('2026. 10. 20'),oldDate:document.body.innerText.includes('2026. 10. 10'),cancelledNext:document.body.innerText.includes('QA 여성 · 다음 연락')})`);

  await evaluate(`navigate('social')`);await waitFor("document.body.innerText.includes('QA 프라이빗 소셜')","dynamic social Event");
  const socialBefore=await evaluate(`({dynamicEvent:document.body.innerText.includes('QA 프라이빗 소셜'),fee:document.body.innerText.includes('50,000원'),hardcoded:/8월\s*29일|9월\s*19일/.test(document.body.innerText),nextIntent:document.body.innerText.includes('다음 모임 희망'),eventOptions:Array.from(document.querySelectorAll('[data-social-event] option')).map(o=>o.textContent)})`);
  await evaluate(`(()=>{const card=document.querySelector('[data-social-item="phone:01033334444"]');card.querySelector('[data-social-status]').value='cancelled';card.querySelector('[data-social-reason]').value='';card.querySelector('[data-social-save]').click()})()`);await waitFor("document.querySelector('.toast')","social cancel reason guard");
  const socialCallsBeforeReason=await evaluate(`window.__qaCalls.filter(c=>c.action==='admin-social-status-set').length`);
  await evaluate(`(()=>{const card=document.querySelector('[data-social-item="phone:01033334444"]');card.querySelector('[data-social-status]').value='payment_pending';card.querySelector('[data-social-save]').click()})()`);await waitFor("document.body.innerText.includes('결제 대기')","social payment pending");
  await evaluate(`(()=>{const card=document.querySelector('[data-social-item="phone:01033334444"]');card.querySelector('[data-social-status]').value='paid';card.querySelector('[data-social-save]').click()})()`);await waitFor("document.body.innerText.includes('결제 완료')","social paid");
  const social=await evaluate(`({statuses:window.__qaCalls.filter(c=>c.action==='admin-social-status-set').map(c=>c.body.status),paidVisible:document.body.innerText.includes('결제 완료')})`);

  await send("Emulation.setDeviceMetricsOverride",{width:390,height:844,deviceScaleFactor:1,mobile:true});await sleep(250);
  await evaluate(`document.querySelector('.mobile-toggle').click()`);await waitFor("document.querySelector('.layout').classList.contains('menu-open')","mobile menu open");
  await sleep(300);
  const mobileOpen=await evaluate(`({expanded:document.querySelector('.mobile-toggle').getAttribute('aria-expanded'),controls:document.querySelector('.mobile-toggle').getAttribute('aria-controls'),focus:document.activeElement?.className,overflow:document.body.style.overflow,labels:Array.from(document.querySelectorAll('[data-nav]')).map(n=>n.textContent.trim())})`);
  await screenshot(`${outputDir}/mobile-menu.png`);
  await evaluate(`document.querySelector('.backdrop').click()`);await waitFor("!document.querySelector('.layout').classList.contains('menu-open')","mobile backdrop close");
  await evaluate(`document.querySelector('.mobile-toggle').click()`);await send("Input.dispatchKeyEvent",{type:"keyDown",key:"Escape",code:"Escape",windowsVirtualKeyCode:27,nativeVirtualKeyCode:27});await send("Input.dispatchKeyEvent",{type:"keyUp",key:"Escape",code:"Escape",windowsVirtualKeyCode:27,nativeVirtualKeyCode:27});await waitFor("!document.querySelector('.layout').classList.contains('menu-open')","mobile escape close");
  await sleep(300);
  const mobile=await evaluate(`({expanded:document.querySelector('.mobile-toggle').getAttribute('aria-expanded'),focus:document.activeElement?.className,bodyOverflow:document.body.style.overflow,horizontalOverflow:document.documentElement.scrollWidth>document.documentElement.clientWidth+1})`);
  await screenshot(`${outputDir}/mobile-social.png`);

  const result={unauthenticated,list,filterQuery,detail,crmDetail,callsBeforeReason,reviewCalls,sentCalls,scheduleTypes,membersBefore,members,candidateOptions,matching,schedules,socialBefore,socialCallsBeforeReason,social,mobileOpen,mobile};
  await writeFile(`${outputDir}/result.json`,`${JSON.stringify(result,null,2)}\n`);
  console.log(JSON.stringify(result));

  if(!unauthenticated.login||unauthenticated.layout)throw new Error("Unauthenticated admin gate failed");
  if(list.cards!==3||list.filters!==9||list.duplicateCards!==1||list.oldMenu||list.overflow)throw new Error("Applicant grouping/filter/navigation desktop QA failed");
  if(filterQuery.count!==filterQuery.cards||!filterQuery.display.startsWith(`${filterQuery.count}명 /`)||!filterQuery.search.includes('secondaryCompletion=submitted')||filterQuery.search.includes('secondaryLink=all'))throw new Error("Applicant filter query/count QA failed");
  if(detail.sections.map(v=>v.join(":" )).join(",")!=="primary-profile:1,secondary-responses:2,consultation-review:3,secondary-links-notes:4,consultation-date:5"||!detail.review||!detail.document||!detail.markSent||!detail.copy||!detail.privatePhoto||detail.currentWork||!detail.koreanAnswers||detail.rawDuplicate||detail.overflow)throw new Error("Applicant five-section/Korean response/private operation QA failed");
  if(!crmDetail.rail||!crmDetail.railText.includes("수정")||!crmDetail.railText.includes("전화상담")||!crmDetail.railText.includes("내부평가")||!crmDetail.railText.includes("첫 만남")||crmDetail.corrections!==1||crmDetail.phone!==1||crmDetail.internal!==1||crmDetail.feedback!==1||crmDetail.primaryJob!=="대표"||crmDetail.feedbackSubject!=="temporary_submission:2"||crmDetail.providerSubject!=="legacy_snapshot:1"||crmDetail.feedbackCase!=="meeting-completed-1"||crmDetail.correctionCalls!==1||crmDetail.phoneCalls!==1||crmDetail.internalCalls!==1)throw new Error("Consultation CRM append-only correction/feedback/history QA failed");
  if(callsBeforeReason!==0||reviewCalls!==2)throw new Error("Review eligibility/reason guard failed");
  if(sentCalls.mark!==1||sentCalls.clear!==1||sentCalls.markBody?.subject_type!=="temporary_submission"||sentCalls.markBody?.subject_id!=="2"||sentCalls.clearBody?.subject_type!=="temporary_submission"||sentCalls.clearBody?.subject_id!=="2")throw new Error("Manual sent ownership QA failed");
  if(scheduleTypes.join(",")!=="consultation,next_contact")throw new Error("Split schedule QA failed");
  if(membersBefore.cards<1||!membersBefore.hasPending||!membersBefore.oldSnapshot||membersBefore.pendingNext!=="converted"||members.before!=="approval_pending"||members.latestMale!=="matchable"||members.latestSocial!=="matchable")throw new Error("Approved member transition QA failed");
  if(!candidateOptions.men.includes("temporary_submission:2")||!candidateOptions.women.includes("legacy_snapshot:1")||matching.cases!==2||matching.hasFemaleDecision||matching.maleLatest!=="matching"||matching.femaleLatest!=="matching")throw new Error("Matchable-only male-choice matching QA failed");
  if(!schedules.newDate||schedules.oldDate||schedules.cancelledNext)throw new Error("Latest schedule projection QA failed");
  if(!socialBefore.dynamicEvent||!socialBefore.fee||socialBefore.hardcoded||socialBefore.eventOptions.every(v=>!v.includes("QA 프라이빗 소셜"))||socialCallsBeforeReason!==0||social.statuses.join(",")!=="payment_pending,paid"||!social.paidVisible)throw new Error("Dynamic social payment flow QA failed");
  const expected=["대시보드","신청자","승인 회원","1:1 매칭","모임 신청 현황","할 일·일정","운영 이력"];
  if(mobileOpen.expanded!=="true"||mobileOpen.controls!=="admin-navigation"||!String(mobileOpen.focus).includes("mobile-close")||mobileOpen.overflow!=="hidden"||expected.some(v=>!mobileOpen.labels.includes(v)))throw new Error("Mobile navigation open QA failed");
  if(mobile.expanded!=="false"||mobile.bodyOverflow!==""||mobile.horizontalOverflow||!String(mobile.focus).includes("mobile-toggle"))throw new Error("Mobile navigation close/layout QA failed");
}finally{socket.close();chromium.kill("SIGTERM");server.close();await sleep(250);await rm(profileDir,{recursive:true,force:true})}
